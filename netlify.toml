[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback: serve index.html for client-side routes like /work/:slug
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
